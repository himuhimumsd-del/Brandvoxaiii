{
  "name": "brandvox-ai-root",
  "version": "1.0.0",
  "private": true,
  "description": "BrandVox AI root configurations - boots client and server in workspace environments using Yarn commands",
  "scripts": {
    "install-all": "yarn --cwd client install && yarn --cwd server install",
    "dev-server": "yarn --cwd server start",
    "dev-client": "yarn --cwd client dev",
    "dev": "concurrently \"yarn dev-server\" \"yarn dev-client\"",
    "render-build": "yarn install-all && yarn --cwd client build",
    "start": "yarn --cwd server start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "license": "UNLICENSED",
  "main": "index.js",
  "repository": "https://github.com/himuhimumsd-del/Brandvoxaiii.git",
  "author": "Himanshu <himuhimumsd@gmail.com>",
  "dependencies": {
    "serverless-http": "^4.0.0"
  }
}
